import { Card, Container } from "reactstrap";

const NotFound = () => {
  return (
    <Container>
      <Card>
        Page could not be found.
      </Card>
    </Container>
  );
};

export default NotFound;